import React, { useEffect, useState } from 'react';

const Profile = () => {

    useEffect(() => {
        window.scrollTo(0,0)
      }, [])

    return (
        <div>This is the profile screen.</div>
    )
}

export default Profile